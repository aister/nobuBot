<div id="main"></div>
<script>
  var HOST = location.origin.replace(/^http/, 'ws')
  var ws = new WebSocket(HOST);
  ws.onmessage = function (event) {
    event = event.data;
    if (event.type == "cfyc") document.getElementById('main').innerHTML = event.img;
  }
  ws.onopen = function () {
    ws.send({type: "cfyc"});
  }
</script>
